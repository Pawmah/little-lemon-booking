// src/api.js
export function fetchAPI(date) {
  // If you already have the Coursera api.js file locally, paste it here.
  // TEMP fallback (so your app never crashes):
  return ["17:00", "18:00", "19:00", "20:00", "21:00"];
}

export function submitAPI(formData) {
  // TEMP fallback (so your app never crashes):
  return true;
}
